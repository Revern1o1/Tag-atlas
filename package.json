{
  "name": "tag-atlas",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate-tag-index.mjs",
    "build:index": "node scripts/build-tag-index.mjs",
    "build:semantic": "node scripts/build-semantic-index.mjs",
    "build": "npm run validate && npm run build:index && npm run build:semantic"
  }
}
